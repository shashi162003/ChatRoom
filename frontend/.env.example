# Frontend Environment Variables

# WebSocket URL for production deployment
# Replace with your actual backend URL when deploying
VITE_WS_URL=wss://your-backend-url.onrender.com

# Development (automatically uses ws://localhost:8080)
# No need to set VITE_WS_URL for local development
